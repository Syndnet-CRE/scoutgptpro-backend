generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         String       @id @default(cuid())
  email      String       @unique
  name       String?
  role       UserRole     @default(AGENT)
  createdAt  DateTime     @default(now())
  updatedAt  DateTime     @updatedAt
  buyBoxes   BuyBox[]
  documents  Document[]
  listings   Listing[]
  mapQueries MapQuery[]
  tasks      Task[]
  profile    UserProfile?

  @@map("users")
}

model UserProfile {
  id            String   @id @default(cuid())
  userId        String   @unique
  phone         String?
  company       String?
  licenseNumber String?
  bio           String?
  avatar        String?
  preferences   Json?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_profiles")
}

model Property {
  id                    String    @id @default(cuid())
  address               String?
  city                  String?
  state                 String?
  zip                   String?
  county                String?
  apn                   String?
  legalDesc             String?
  propertyType          String?
  size                  Float?
  sizeUnit              String?
  zoning                String?
  yearBuilt             Int?
  assessedValue         Float?
  marketValue           Float?
  ownerName             String?
  ownerAddress          String?
  metadata              Json?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  acres                 Float?
  centroid              Json?
  impValue              Float?
  isAbsentee            Boolean   @default(false)
  isTaxDelinquent       Boolean   @default(false)
  isVacantLand          Boolean   @default(false)
  landValue             Float?
  latitude              Float?
  longitude             Float?
  mailingAddr           String?
  mktValue              Float?
  motivationScore       Int?
  opportunityFlags      String[]
  owner                 String?
  parcelId              String    @unique
  siteAddress           String?
  taxYear               Int?
  totalDue              Float?
  totalTax              Float?
  siteCity              String?
  siteState             String?
  siteZip               String?
  appraisedValue        Float?
  block                 String?
  deedDate              String?
  enrichedAt            DateTime?
  enrichmentSource      String?
  floodZone             String?
  geoId                 String?
  landTypeDesc          String?
  lot                   String?
  ownerFirstName        String?
  ownerLastName         String?
  situsNum              String?
  situsStreet           String?
  subdivision           String?
  tcadAcres             Float?
  attomId               String?   @map("attomId")
  avmValue              Decimal?  @map("avmValue") @db.Decimal(14, 2)
  avmMin                Decimal?  @map("avmMin") @db.Decimal(14, 2)
  avmMax                Decimal?  @map("avmMax") @db.Decimal(14, 2)
  avmConfidence         Int?      @map("avmConfidence")
  avmDate               DateTime? @map("avmDate")
  granteeMailAddress    String?
  granteeMailCity       String?
  granteeMailState      String?
  granteeMailZip        String?
  granteeName           String?
  grantorName           String?
  isForeclosure         Boolean   @default(false)
  isInvestorOwned       Boolean   @default(false)
  lastSaleAmount        Float?
  lastSaleDate          DateTime?
  lastSaleDocType       String?
  mortgageAmount        Float?
  mortgageLender        String?
  mortgageRate          Float?
  mortgageTerm          Int?
  ownershipYears        Float?
  asset_class           String?
  land_use_code         String?   @db.VarChar(10)
  general_land_use_code String?   @db.VarChar(10)
  asset_subtype         String?   @db.VarChar(50)
  land_use              String?   @db.VarChar(20)
  general_land_use      String?   @db.VarChar(20)
  comps                 Comp[]
  deals                 Deal[]
  listings              Listing[]
  pins                  Pin[]

  @@index([propertyType])
  @@index([latitude, longitude])
  @@index([isAbsentee])
  @@index([isTaxDelinquent])
  @@index([motivationScore])
  @@index([acres])
  @@index([totalTax])
  @@index([parcelId])
  @@index([attomId])
  @@index([asset_class], map: "idx_properties_asset_class")
  @@map("properties")
}

model Listing {
  id            String        @id @default(cuid())
  status        ListingStatus @default(ACTIVE)
  propertyType  PropertyType
  title         String
  description   String?
  address       String
  city          String
  state         String        @default("TX")
  zipCode       String
  county        String?
  latitude      Float?
  longitude     Float?
  apn           String?
  askingPrice   Decimal       @db.Decimal(15, 2)
  pricePerSqft  Decimal?      @db.Decimal(10, 2)
  pricePerAcre  Decimal?      @db.Decimal(15, 2)
  totalSqft     Int?
  lotSizeAcres  Decimal?      @db.Decimal(10, 4)
  lotSizeSqft   Int?
  yearBuilt     Int?
  zoning        String?
  assetType     String?
  assetSubtype  String?
  noi           Decimal?      @db.Decimal(15, 2)
  capRate       Decimal?      @db.Decimal(5, 2)
  occupancy     Decimal?      @db.Decimal(5, 2)
  tenantCount   Int?
  buildingCount Int?
  floors        Int?
  parkingSpaces Int?
  leaseType     String?
  bedrooms      Int?
  bathrooms     Decimal?      @db.Decimal(3, 1)
  hoaFee        Decimal?      @db.Decimal(10, 2)
  totalAcres    Decimal?      @db.Decimal(10, 4)
  numberOfLots  Int?
  roadFrontage  Int?
  topography    String?
  utilities     Json?
  entitlements  String?
  images        Json?
  documents     Json?
  coverImage    String?
  views         Int           @default(0)
  inquiries     Int           @default(0)
  userId        String?
  propertyId    String?
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  listedAt      DateTime      @default(now())
  property      Property?     @relation(fields: [propertyId], references: [id])
  user          User?         @relation(fields: [userId], references: [id])

  @@index([status])
  @@index([propertyType])
  @@index([city])
  @@index([askingPrice])
  @@map("listings")
}

model Deal {
  id            String     @id @default(cuid())
  userId        String
  propertyId    String?
  title         String
  stage         DealStage  @default(inbound)
  dealType      String?
  purchasePrice Float?
  offerPrice    Float?
  closingDate   DateTime?
  seller        String?
  buyer         String?
  probability   Int?
  lostReason    String?
  notes         String?
  metadata      Json?
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  activities    Activity[]
  property      Property?  @relation(fields: [propertyId], references: [id])
  documents     Document[]
  tasks         Task[]

  @@index([userId, stage])
  @@index([stage])
  @@map("deals")
}

model DealRoom {
  id                  String   @id @default(uuid()) @db.Uuid
  ownerId             String
  title               String
  status              String   @default("inbound")
  assetType           String?
  location            String?
  purchasePrice       Float?
  capEx               Float?
  targetCapRate       Float?
  notes               String?
  characteristics     Json?
  outputs             Json?
  mapState            Json?
  createdAt           DateTime @default(now()) @map("created_at")
  updatedAt           DateTime @updatedAt @map("updated_at")
  primaryPropertyId   String?
  propertyIds         String[] @default([])
  accessTier          String   @default("PUBLIC_PREVIEW")
  acres               Float?
  address             String?
  addressMasked       String?
  allowedUses         String[] @default([])
  askingPrice         Float?
  assetClass          String?
  capRate             Float?
  capacityNotes       String?
  contacts            Json?
  county              String?
  description         String?
  developmentNotes    String?
  easementsNotes      String?
  electricProvider    String?
  entitlementStatus   String?
  environmentalIssues String?
  far                 Float?
  fiberAvailable      Boolean?
  floodZone           String?
  gasProvider         String?
  highlights          String[] @default([])
  knownConstraints    String?
  legalDescription    String?
  locationVisibility  String   @default("exact")
  maxHeight           Float?
  ndaRequired         Boolean  @default(false)
  noi                 Float?
  occupancy           Float?
  offeringType        String?
  parcelIds           String[] @default([])
  phaseIIStatus       String?
  phaseIStatus        String?
  physicalAttributes  Json?
  propertyType        String?
  requiredStudies     String[] @default([])
  setbacks            Json?
  sewerProvider       String?
  soilNotes           String?
  squareFeet          Float?
  submarket           String?
  t12Expenses         Float?
  t12Income           Float?
  topTenantSummary    String?
  units               Int?
  waterProvider       String?
  wetlandsPresent     String?
  yearBuilt           Int?
  zoning              String?
  zoningCodes         String[] @default([])
  zoningJurisdiction  String?

  @@index([ownerId])
  @@map("deal_rooms")
}

model DealDocument {
  id          String   @id @default(cuid())
  dealRoomId  String
  folder      String
  name        String
  fileUrl     String
  fileType    String?
  fileSize    Int?
  accessTier  String   @default("REGISTERED")
  canDownload Boolean  @default(true)
  uploadedBy  String
  createdAt   DateTime @default(now())

  @@index([dealRoomId])
  @@map("deal_documents")
}

model DealMedia {
  id           String   @id @default(cuid())
  dealRoomId   String
  mediaType    String
  tag          String?
  name         String?
  url          String
  thumbnailUrl String?
  uploadedBy   String
  createdAt    DateTime @default(now())

  @@index([dealRoomId])
  @@map("deal_media")
}

model DealUserAccess {
  id          String    @id @default(cuid())
  dealRoomId  String
  userId      String
  accessTier  String    @default("PUBLIC_PREVIEW")
  ndaStatus   String    @default("NOT_REQUESTED")
  requestedAt DateTime?
  approvedAt  DateTime?
  approvedBy  String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@unique([dealRoomId, userId])
  @@index([dealRoomId])
  @@index([userId])
  @@map("deal_user_access")
}

model NdaSignature {
  id             String   @id @default(cuid())
  dealRoomId     String
  userId         String
  signerName     String
  signerEmail    String?
  signedAt       DateTime @default(now())
  ipAddress      String?
  ndaVersionHash String?

  @@index([dealRoomId])
  @@index([userId])
  @@map("nda_signatures")
}

model DealActivityLog {
  id         String   @id @default(cuid())
  dealRoomId String
  userId     String
  action     String
  details    Json?
  createdAt  DateTime @default(now())

  @@index([dealRoomId])
  @@index([userId])
  @@map("deal_activity_log")
}

model BuyerAssumptions {
  id              String   @id @default(cuid())
  dealRoomId      String
  userId          String
  purchasePrice   Float?
  capExAssumption Float?
  exitCap         Float?
  holdPeriod      Int?
  notes           String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([dealRoomId, userId])
  @@index([dealRoomId])
  @@index([userId])
  @@map("buyer_assumptions")
}

model BuyBox {
  id            String   @id @default(cuid())
  userId        String
  name          String
  isActive      Boolean  @default(true)
  markets       Json?
  counties      Json?
  propertyTypes Json?
  minSize       Float?
  maxSize       Float?
  sizeUnit      String?
  minPrice      Float?
  maxPrice      Float?
  minCap        Float?
  maxCap        Float?
  zoning        Json?
  minYearBuilt  Int?
  filters       Json?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("buy_boxes")
}

model Document {
  id          String   @id @default(cuid())
  userId      String
  dealId      String?
  filename    String
  fileUrl     String
  fileType    String
  fileSize    Int
  category    String?
  description String?
  uploadedAt  DateTime @default(now())
  deal        Deal?    @relation(fields: [dealId], references: [id], onDelete: Cascade)
  users       User     @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([dealId])
  @@map("documents")
}

model Activity {
  id          String   @id @default(cuid())
  userId      String
  dealId      String?
  type        String
  subject     String
  description String?
  completedAt DateTime @default(now())
  deal        Deal?    @relation(fields: [dealId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([dealId])
  @@map("activities")
}

model Task {
  id          String     @id @default(cuid())
  userId      String
  dealId      String?
  title       String
  description String?
  dueDate     DateTime?
  priority    Priority   @default(MEDIUM)
  status      TaskStatus @default(TODO)
  completedAt DateTime?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  deal        Deal?      @relation(fields: [dealId], references: [id], onDelete: Cascade)
  user        User       @relation(fields: [userId], references: [id])

  @@index([userId, status])
  @@index([dueDate])
  @@map("tasks")
}

model Comp {
  id           String    @id @default(cuid())
  propertyId   String?
  address      String
  city         String
  state        String
  propertyType String
  size         Float
  sizeUnit     String
  salePrice    Float
  pricePerUnit Float
  saleDate     DateTime
  capRate      Float?
  noi          Float?
  distance     Float?
  source       String?
  verified     Boolean   @default(false)
  metadata     Json?
  createdAt    DateTime  @default(now())
  property     Property? @relation(fields: [propertyId], references: [id])

  @@index([city, state, propertyType])
  @@map("comps")
}

model GisLayer {
  id         String   @id @default(cuid())
  name       String
  category   String
  sourceType String
  sourceUrl  String?
  style      Json?
  minZoom    Float?
  maxZoom    Float?
  isActive   Boolean  @default(true)
  metadata   Json?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@map("gis_layers")
}

model Pin {
  id         String    @id @default(cuid())
  propertyId String?
  title      String
  lat        Float
  lng        Float
  summary    String?
  tags       Json?
  pinType    String?
  metadata   Json?
  createdAt  DateTime  @default(now())
  property   Property? @relation(fields: [propertyId], references: [id])

  @@index([lat, lng])
  @@map("pins")
}

model MapServerRegistry {
  id              String    @id @default(cuid())
  url             String    @unique
  category        String
  context         String?
  datasetType     String?
  datasetCategory String?
  serviceName     String?
  layerId         Int?
  geometryType    String?
  fields          Json?
  extent          Json?
  isActive        Boolean   @default(true)
  lastQueried     DateTime?
  queryCount      Int       @default(0)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([category])
  @@index([datasetType])
  @@index([isActive, category])
  @@map("map_server_registry")
}

model LayerSet {
  id                String    @id @default(cuid())
  layerSetId        String    @unique
  name              String
  category          String
  description       String?
  geometryType      String
  style             Json
  primaryLayerUrl   String
  primaryLayerId    String?
  alternativeLayers Json?
  totalFeatureCount Int       @default(0)
  layerCount        Int       @default(1)
  isActive          Boolean   @default(true)
  queryCount        Int       @default(0)
  lastQueried       DateTime?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@index([category])
  @@index([geometryType])
  @@index([isActive, category])
  @@map("layer_sets")
}

model MapQuery {
  id              String   @id @default(cuid())
  userId          String
  query           String
  selectedServers Json
  bounds          Json?
  results         Json?
  createdAt       DateTime @default(now())
  user            User     @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([createdAt])
  @@map("map_queries")
}

model PolygonSearch {
  id             String   @id @default(cuid())
  userId         String?
  name           String
  description    String?
  polygonGeoJSON Json
  areaAcres      Float?
  centroidLat    Float?
  centroidLng    Float?
  messages       Json     @default("[]")
  filters        Json?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  lastAccessedAt DateTime @default(now())
  isArchived     Boolean  @default(false)

  @@index([userId])
  @@index([updatedAt])
  @@index([lastAccessedAt])
  @@map("polygon_searches")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model spatial_ref_sys {
  srid      Int     @id
  auth_name String? @db.VarChar(256)
  auth_srid Int?
  srtext    String? @db.VarChar(2048)
  proj4text String? @db.VarChar(2048)
}

model Owner {
  id                 String           @id @default(cuid())
  ownerNameRaw       String?
  ownerNameNorm      String?
  mailingAddressRaw  String?
  mailingAddressNorm String?
  mailingState       String?
  entityType         EntityType       @default(UNKNOWN)
  isCorporate        Boolean          @default(false)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @default(now()) @updatedAt
  features           OwnerFeaturesTx?
  properties         OwnerProperty[]

  @@index([entityType], map: "idx_owners_entitytype")
  @@index([isCorporate], map: "idx_owners_iscorporate")
  @@index([mailingState], map: "idx_owners_mailingstate")
  @@index([ownerNameNorm], map: "idx_owners_ownernamenorm")
  @@map("owners")
}

model OwnerProperty {
  id        String   @id @default(cuid())
  ownerId   String
  parcelId  String
  createdAt DateTime @default(now())
  owner     Owner    @relation(fields: [ownerId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([ownerId, parcelId])
  @@index([ownerId], map: "idx_owner_properties_ownerid")
  @@index([parcelId], map: "idx_owner_properties_parcelid")
  @@map("owner_properties")
}

model OwnerFeaturesTx {
  id                   String   @id
  ownerId              String   @unique
  parcelCountTx        Int      @default(0)
  totalAssessedValueTx Decimal? @db.Decimal(15, 2)
  assetClassMix        Json?
  absenteeRate         Decimal? @db.Decimal(5, 4)
  outOfState           Boolean  @default(false)
  avgHoldYears         Decimal? @db.Decimal(5, 2)
  updatedAt            DateTime @default(now()) @updatedAt
  owner                Owner    @relation(fields: [ownerId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([outOfState], map: "idx_owner_features_tx_outofstate")
  @@index([parcelCountTx], map: "idx_owner_features_tx_parcelcounttx")
  @@map("owner_features_tx")
}

model OwnerSegment {
  segmentKey  String   @id
  description String?
  ruleJson    Json
  version     String   @default("1.0")
  updatedAt   DateTime @default(now()) @updatedAt

  @@index([segmentKey], map: "idx_owner_segments_segmentkey")
  @@map("owner_segments")
}

model TxEnrichmentRollup {
  parcelId     String   @id
  pop1mi       Decimal? @db.Decimal(10, 0)
  medIncome1mi Decimal? @db.Decimal(10, 2)
  poiCounts    Json?
  nearestPoi   Json?
  trafficIndex Decimal? @db.Decimal(5, 2)
  floodPct     Decimal? @db.Decimal(5, 4)
  inFloodplain Boolean  @default(false)
  updatedAt    DateTime @default(now()) @updatedAt

  @@index([parcelId], map: "idx_tx_enrichment_rollups_parcelid")
  @@map("tx_enrichment_rollups")
}

model DiscoverRun {
  id         String           @id @default(cuid())
  queryText  String?
  intentJson Json
  createdBy  String?
  startedAt  DateTime         @default(now())
  endedAt    DateTime?
  stats      Json?
  error      String?
  results    DiscoverResult[]

  @@index([createdBy], map: "idx_discover_runs_createdby")
  @@index([startedAt], map: "idx_discover_runs_startedat")
  @@map("discover_runs")
}

model DiscoverResult {
  id        String      @id @default(cuid())
  runId     String
  parcelId  String
  score     Decimal     @db.Decimal(10, 6)
  reasons   Json
  breakdown Json
  run       DiscoverRun @relation(fields: [runId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([parcelId], map: "idx_discover_results_parcelid")
  @@index([runId], map: "idx_discover_results_runid")
  @@index([score], map: "idx_discover_results_score")
  @@map("discover_results")
}

model ScoringModel {
  modelId    String   @id
  assetClass String
  version    String   @default("1.0")
  modelJson  Json
  updatedAt  DateTime @default(now()) @updatedAt

  @@index([assetClass], map: "idx_scoring_models_assetclass")
  @@index([version], map: "idx_scoring_models_version")
  @@map("scoring_models")
}

model austin_land_use {
  property_id      String  @id @db.VarChar(20)
  land_use         String? @db.VarChar(10)
  general_land_use String? @db.VarChar(10)
}

model buyboxes {
  id         String   @id
  name       String
  intentJson Json
  createdBy  String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @default(now())

  @@index([createdBy], map: "idx_buyboxes_createdby")
}

model county_config {
  fips             String    @id
  county_name      String
  table_name       String
  enrichment_table String
  mts_tileset_id   String?
  enabled          Boolean?  @default(true)
  parcel_count     Int?
  last_import      DateTime? @db.Timestamptz(6)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
}

model land_use_codes {
  code        String  @id @db.VarChar(20)
  description String? @db.VarChar(100)
  category    String? @db.VarChar(50)
}

model osm_pois_travis {
  id          Int                      @id @default(autoincrement())
  osm_id      BigInt                   @unique
  name        String?
  category    String
  subcategory String?
  latitude    Float
  longitude   Float
  address     String?
  city        String?
  state       String?                  @default("TX")
  zip         String?
  phone       String?
  website     String?
  tags        Json?
  created_at  DateTime?                @default(now()) @db.Timestamp(6)
  updated_at  DateTime?                @default(now()) @db.Timestamp(6)
  geom        Unsupported("geometry")?
  property_id String?

  @@index([category, subcategory], map: "idx_osm_pois_travis_category")
  @@index([geom], map: "idx_osm_pois_travis_geom", type: Gist)
  @@index([osm_id], map: "idx_osm_pois_travis_osm_id")
  @@index([property_id], map: "idx_osm_pois_travis_property_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model parcel_features_travis {
  parcel_id                    String                   @id
  county_fips                  String                   @default("48453")
  situs_address                String?
  mailing_address              String?
  mail_city                    String?
  mail_state                   String?
  mail_zip                     String?
  owner_name_raw               String?
  owner_name_norm              String?
  owner_entity_type            String?
  owner_portfolio_count_travis Int?                     @default(0)
  owner_segment                String?
  acres_calc                   Decimal                  @db.Decimal
  acres_calc_source            String                   @default("enrichment.acreage")
  acres_calc_confidence        Decimal?                 @default(1.0) @db.Decimal
  asset_class                  String?
  asset_class_confidence       Decimal?                 @db.Decimal
  year_built                   Int?
  building_sqft                Decimal?                 @db.Decimal
  market_value                 Decimal?                 @db.Decimal
  assessed_total_value         Decimal?                 @db.Decimal
  land_value                   Decimal?                 @db.Decimal
  improvement_value            Decimal?                 @db.Decimal
  tax_delinquent_flag          Boolean?                 @default(false)
  homestead_exemption_flag     Boolean?                 @default(false)
  last_sale_date               DateTime?                @db.Date
  last_sale_price              Decimal?                 @db.Decimal
  zoning_code                  String?
  flood_zone                   String?
  land_use_code                String?
  land_use_desc                String?
  geom_centroid                Unsupported("geometry")?
  created_at                   DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime                 @default(now()) @db.Timestamptz(6)
  opportunities                opportunities?
  signals                      signals[]

  @@index([acres_calc], map: "idx_pft_acres")
  @@index([asset_class], map: "idx_pft_asset_class")
  @@index([county_fips, acres_calc], map: "idx_pft_county_acres")
  @@index([geom_centroid], map: "idx_pft_geom", type: Gist)
  @@index([market_value], map: "idx_pft_market_value")
  @@index([owner_entity_type], map: "idx_pft_owner_entity_type")
  @@index([owner_segment], map: "idx_pft_owner_segment")
}

model parcels_bastrop {
  parcel_id                  String                      @id
  geom                       Unsupported("geometry")
  created_at                 DateTime                    @default(now()) @db.Timestamptz(6)
  parcels_bastrop_enrichment parcels_bastrop_enrichment?

  @@index([geom], type: Gist)
}

model parcels_bastrop_enrichment {
  parcel_id                String          @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?        @db.Decimal
  improvement_value        Decimal?        @db.Decimal
  market_value             Decimal?        @db.Decimal
  assessed_value           Decimal?        @db.Decimal
  acres                    Decimal?        @db.Decimal
  acreage                  Decimal?        @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?       @db.Date
  last_sale_price          Decimal?        @db.Decimal
  tax_delinquent_flag      Boolean?        @default(false)
  homestead_exemption_flag Boolean?        @default(false)
  created_at               DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?       @default(now()) @db.Timestamptz(6)
  parcels_bastrop          parcels_bastrop @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_bell {
  parcel_id               String                   @id
  geom                    Unsupported("geometry")
  created_at              DateTime                 @default(now()) @db.Timestamptz(6)
  parcels_bell_enrichment parcels_bell_enrichment?

  @@index([geom], type: Gist)
}

model parcels_bell_enrichment {
  parcel_id                String       @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?     @db.Decimal
  improvement_value        Decimal?     @db.Decimal
  market_value             Decimal?     @db.Decimal
  assessed_value           Decimal?     @db.Decimal
  acres                    Decimal?     @db.Decimal
  acreage                  Decimal?     @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?    @db.Date
  last_sale_price          Decimal?     @db.Decimal
  tax_delinquent_flag      Boolean?     @default(false)
  homestead_exemption_flag Boolean?     @default(false)
  created_at               DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?    @default(now()) @db.Timestamptz(6)
  parcels_bell             parcels_bell @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_blanco {
  parcel_id                 String                     @id
  geom                      Unsupported("geometry")
  created_at                DateTime                   @default(now()) @db.Timestamptz(6)
  parcels_blanco_enrichment parcels_blanco_enrichment?

  @@index([geom], type: Gist)
}

model parcels_blanco_enrichment {
  parcel_id                String         @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?       @db.Decimal
  improvement_value        Decimal?       @db.Decimal
  market_value             Decimal?       @db.Decimal
  assessed_value           Decimal?       @db.Decimal
  acres                    Decimal?       @db.Decimal
  acreage                  Decimal?       @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?      @db.Date
  last_sale_price          Decimal?       @db.Decimal
  tax_delinquent_flag      Boolean?       @default(false)
  homestead_exemption_flag Boolean?       @default(false)
  created_at               DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?      @default(now()) @db.Timestamptz(6)
  parcels_blanco           parcels_blanco @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_burnet {
  parcel_id                 String                     @id
  geom                      Unsupported("geometry")
  created_at                DateTime                   @default(now()) @db.Timestamptz(6)
  parcels_burnet_enrichment parcels_burnet_enrichment?

  @@index([geom], type: Gist)
}

model parcels_burnet_enrichment {
  parcel_id                String         @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?       @db.Decimal
  improvement_value        Decimal?       @db.Decimal
  market_value             Decimal?       @db.Decimal
  assessed_value           Decimal?       @db.Decimal
  acres                    Decimal?       @db.Decimal
  acreage                  Decimal?       @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?      @db.Date
  last_sale_price          Decimal?       @db.Decimal
  tax_delinquent_flag      Boolean?       @default(false)
  homestead_exemption_flag Boolean?       @default(false)
  created_at               DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?      @default(now()) @db.Timestamptz(6)
  parcels_burnet           parcels_burnet @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_burnet_raw {
  gid        Int                      @id @default(autoincrement())
  prop_id    String?                  @db.VarChar
  geo_id     String?                  @db.VarChar
  owner_name String?                  @db.VarChar
  name_care  String?                  @db.VarChar
  legal_area Float?
  lgl_area_u String?                  @db.VarChar
  gis_area   Float?
  gis_area_u String?                  @db.VarChar
  legal_desc String?                  @db.VarChar
  stat_land_ String?                  @db.VarChar
  loc_land_u String?                  @db.VarChar
  land_value Float?
  imp_value  Float?
  mkt_value  Float?
  situs_addr String?                  @db.VarChar
  situs_num  String?                  @db.VarChar
  situs_stre String?                  @db.VarChar
  situs_st_1 String?                  @db.VarChar
  situs_st_2 String?                  @db.VarChar
  situs_city String?                  @db.VarChar
  situs_stat String?                  @db.VarChar
  situs_zip  String?                  @db.VarChar
  mail_addr  String?                  @db.VarChar
  mail_line1 String?                  @db.VarChar
  mail_line2 String?                  @db.VarChar
  mail_city  String?                  @db.VarChar
  mail_stat  String?                  @db.VarChar
  mail_zip   String?                  @db.VarChar
  source     String?                  @db.VarChar
  date_acq   BigInt?
  fips       String?                  @db.VarChar
  county     String?                  @db.VarChar
  tax_year   BigInt?
  year_built String?                  @db.VarChar
  objectid_1 BigInt?
  shape_leng Float?
  shape_area Float?
  geom       Unsupported("geometry")?

  @@index([geom], map: "parcels_burnet_raw_geom_geom_idx", type: Gist)
}

model parcels_caldwell {
  parcel_id                   String                       @id
  geom                        Unsupported("geometry")
  created_at                  DateTime                     @default(now()) @db.Timestamptz(6)
  parcels_caldwell_enrichment parcels_caldwell_enrichment?

  @@index([geom], type: Gist)
}

model parcels_caldwell_enrichment {
  parcel_id                String           @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?         @db.Decimal
  improvement_value        Decimal?         @db.Decimal
  market_value             Decimal?         @db.Decimal
  assessed_value           Decimal?         @db.Decimal
  acres                    Decimal?         @db.Decimal
  acreage                  Decimal?         @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?        @db.Date
  last_sale_price          Decimal?         @db.Decimal
  tax_delinquent_flag      Boolean?         @default(false)
  homestead_exemption_flag Boolean?         @default(false)
  created_at               DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?        @default(now()) @db.Timestamptz(6)
  parcels_caldwell         parcels_caldwell @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_comal {
  parcel_id                String                    @id
  geom                     Unsupported("geometry")
  created_at               DateTime                  @default(now()) @db.Timestamptz(6)
  parcels_comal_enrichment parcels_comal_enrichment?

  @@index([geom], type: Gist)
}

model parcels_comal_enrichment {
  parcel_id                String        @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?      @db.Decimal
  improvement_value        Decimal?      @db.Decimal
  market_value             Decimal?      @db.Decimal
  assessed_value           Decimal?      @db.Decimal
  acres                    Decimal?      @db.Decimal
  acreage                  Decimal?      @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?     @db.Date
  last_sale_price          Decimal?      @db.Decimal
  tax_delinquent_flag      Boolean?      @default(false)
  homestead_exemption_flag Boolean?      @default(false)
  created_at               DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?     @default(now()) @db.Timestamptz(6)
  parcels_comal            parcels_comal @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_hays {
  parcel_id               String                   @id
  geom                    Unsupported("geometry")
  created_at              DateTime                 @default(now()) @db.Timestamptz(6)
  parcels_hays_enrichment parcels_hays_enrichment?

  @@index([geom], type: Gist)
}

model parcels_hays_enrichment {
  parcel_id                String       @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?     @db.Decimal
  improvement_value        Decimal?     @db.Decimal
  market_value             Decimal?     @db.Decimal
  assessed_value           Decimal?     @db.Decimal
  acres                    Decimal?     @db.Decimal
  acreage                  Decimal?     @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?    @db.Date
  last_sale_price          Decimal?     @db.Decimal
  tax_delinquent_flag      Boolean?     @default(false)
  homestead_exemption_flag Boolean?     @default(false)
  created_at               DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?    @default(now()) @db.Timestamptz(6)
  parcels_hays             parcels_hays @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_kendall {
  parcel_id                  String                      @id
  geom                       Unsupported("geometry")
  created_at                 DateTime                    @default(now()) @db.Timestamptz(6)
  parcels_kendall_enrichment parcels_kendall_enrichment?

  @@index([geom], type: Gist)
}

model parcels_kendall_enrichment {
  parcel_id                String          @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?        @db.Decimal
  improvement_value        Decimal?        @db.Decimal
  market_value             Decimal?        @db.Decimal
  assessed_value           Decimal?        @db.Decimal
  acres                    Decimal?        @db.Decimal
  acreage                  Decimal?        @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?       @db.Date
  last_sale_price          Decimal?        @db.Decimal
  tax_delinquent_flag      Boolean?        @default(false)
  homestead_exemption_flag Boolean?        @default(false)
  created_at               DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?       @default(now()) @db.Timestamptz(6)
  parcels_kendall          parcels_kendall @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_lee {
  parcel_id              String                  @id
  geom                   Unsupported("geometry")
  created_at             DateTime                @default(now()) @db.Timestamptz(6)
  parcels_lee_enrichment parcels_lee_enrichment?

  @@index([geom], type: Gist)
}

model parcels_lee_enrichment {
  parcel_id                String      @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?    @db.Decimal
  improvement_value        Decimal?    @db.Decimal
  market_value             Decimal?    @db.Decimal
  assessed_value           Decimal?    @db.Decimal
  acres                    Decimal?    @db.Decimal
  acreage                  Decimal?    @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?   @db.Date
  last_sale_price          Decimal?    @db.Decimal
  tax_delinquent_flag      Boolean?    @default(false)
  homestead_exemption_flag Boolean?    @default(false)
  created_at               DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?   @default(now()) @db.Timestamptz(6)
  parcels_lee              parcels_lee @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

model parcels_llano {
  parcel_id                String                    @id
  geom                     Unsupported("geometry")
  created_at               DateTime                  @default(now()) @db.Timestamptz(6)
  parcels_llano_enrichment parcels_llano_enrichment?

  @@index([geom], type: Gist)
}

model parcels_llano_enrichment {
  parcel_id                String        @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?      @db.Decimal
  improvement_value        Decimal?      @db.Decimal
  market_value             Decimal?      @db.Decimal
  assessed_value           Decimal?      @db.Decimal
  acres                    Decimal?      @db.Decimal
  acreage                  Decimal?      @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?     @db.Date
  last_sale_price          Decimal?      @db.Decimal
  tax_delinquent_flag      Boolean?      @default(false)
  homestead_exemption_flag Boolean?      @default(false)
  created_at               DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?     @default(now()) @db.Timestamptz(6)
  parcels_llano            parcels_llano @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model parcels_travis {
  parcel_id                 String                     @id
  geom                      Unsupported("geometry")
  created_at                DateTime                   @default(now()) @db.Timestamptz(6)
  parcels_travis_enrichment parcels_travis_enrichment?

  @@index([geom], type: Gist)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model parcels_travis_centroids {
  parcel_id String?
  centroid  Unsupported("geography")?

  @@index([centroid], map: "idx_travis_centroids", type: Gist)
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model parcels_travis_enrichment {
  parcel_id                  String         @id
  owner_name                 String?
  owner2                     String?
  mail_address1              String?
  mail_address2              String?
  mail_city                  String?
  mail_state                 String?
  mail_zip                   String?
  situs_address              String?
  land_use                   String?
  land_use_desc              String?
  legal_desc                 String?
  year_built                 Int?
  acres                      Decimal?       @db.Decimal
  land_value                 Decimal?       @db.Decimal
  improvement_value          Decimal?       @db.Decimal
  market_value               Decimal?       @db.Decimal
  assessed_value             Decimal?       @db.Decimal
  last_update                DateTime?      @db.Date
  source_layer               String?
  raw                        Json?
  updated_at                 DateTime       @default(now()) @db.Timestamptz(6)
  ingested_at                DateTime?      @db.Timestamptz(6)
  owner_type                 String?
  mailing_address            String?
  land_use_code              String?
  land_use_description       String?
  assessed_land_value        Decimal?       @db.Decimal
  assessed_improvement_value Decimal?       @db.Decimal
  assessed_total_value       Decimal?       @db.Decimal
  acreage                    Decimal?       @db.Decimal
  zoning_code                String?
  flood_zone                 String?
  tax_delinquent_flag        Boolean?
  last_sale_date             DateTime?      @db.Date
  last_sale_price            Decimal?       @db.Decimal
  homestead_exemption_flag   Boolean?
  parcels_travis             parcels_travis @relation(fields: [parcel_id], references: [parcel_id], onDelete: Cascade, onUpdate: NoAction)

  @@index([land_use])
  @@index([market_value])
  @@index([owner_name])
  @@index([year_built])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model parcels_travis_enrichment_stage {
  id          BigInt   @id @default(autoincrement())
  raw         Json
  detected_id String?
  ingested_at DateTime @default(now()) @db.Timestamptz(6)

  @@index([detected_id])
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model parcels_travis_txgio_stage {
  ogc_fid    Int                      @id @default(autoincrement())
  prop_id    String?                  @db.VarChar(50)
  geo_id     String?                  @db.VarChar(50)
  owner_name String?                  @db.VarChar(254)
  name_care  String?                  @db.VarChar(254)
  legal_area String?                  @db.VarChar(20)
  lgl_area_u String?                  @db.VarChar(8)
  gis_area   Decimal?                 @db.Decimal(18, 8)
  gis_area_u String?                  @db.VarChar(8)
  legal_desc String?                  @db.VarChar(254)
  stat_land_ String?                  @db.VarChar(5)
  loc_land_u String?                  @db.VarChar(5)
  land_value Decimal?                 @db.Decimal(18, 8)
  imp_value  Decimal?                 @db.Decimal(18, 8)
  mkt_value  Decimal?                 @db.Decimal(18, 8)
  situs_addr String?                  @db.VarChar(254)
  situs_num  String?                  @db.VarChar(15)
  situs_stre String?                  @db.VarChar(10)
  situs_st_1 String?                  @db.VarChar(60)
  situs_st_2 String?                  @db.VarChar(60)
  situs_city String?                  @db.VarChar(60)
  situs_stat String?                  @db.VarChar(2)
  situs_zip  String?                  @db.VarChar(5)
  mail_addr  String?                  @db.VarChar(254)
  mail_line1 String?                  @db.VarChar(60)
  mail_line2 String?                  @db.VarChar(60)
  mail_city  String?                  @db.VarChar(60)
  mail_stat  String?                  @db.VarChar(2)
  mail_zip   String?                  @db.VarChar(5)
  source     String?                  @db.VarChar(100)
  date_acq   Decimal?                 @db.Decimal(10, 0)
  fips       String?                  @db.VarChar(5)
  county     String?                  @db.VarChar(60)
  tax_year   Decimal?                 @db.Decimal(10, 0)
  year_built String?                  @db.VarChar(60)
  geom       Unsupported("geometry")?

  @@index([geom], map: "parcels_travis_txgio_stage_geom_geom_idx", type: Gist)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model parcels_tx {
  parcel_uid   String                  @id
  geom         Unsupported("geometry")
  state_fips   String
  county_fips  String
  prop_id      String?
  geo_id       String?
  source_layer String?
  ingested_at  DateTime                @default(now()) @db.Timestamptz(6)
  updated_at   DateTime                @default(now()) @db.Timestamptz(6)

  @@index([county_fips])
  @@index([county_fips, state_fips], map: "parcels_tx_county_state_idx")
  @@index([geom], type: Gist)
  @@index([state_fips])
}

model parcels_williamson {
  parcel_id                     String                         @id
  geom                          Unsupported("geometry")
  created_at                    DateTime                       @default(now()) @db.Timestamptz(6)
  parcels_williamson_enrichment parcels_williamson_enrichment?

  @@index([geom], type: Gist)
}

model parcels_williamson_enrichment {
  parcel_id                String             @id
  owner_name               String?
  owner2                   String?
  owner_type               String?
  mail_address1            String?
  mail_address2            String?
  mail_city                String?
  mail_state               String?
  mail_zip                 String?
  situs_address            String?
  land_value               Decimal?           @db.Decimal
  improvement_value        Decimal?           @db.Decimal
  market_value             Decimal?           @db.Decimal
  assessed_value           Decimal?           @db.Decimal
  acres                    Decimal?           @db.Decimal
  acreage                  Decimal?           @db.Decimal
  year_built               Int?
  legal_desc               String?
  zoning_code              String?
  flood_zone               String?
  last_sale_date           DateTime?          @db.Date
  last_sale_price          Decimal?           @db.Decimal
  tax_delinquent_flag      Boolean?           @default(false)
  homestead_exemption_flag Boolean?           @default(false)
  created_at               DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?          @default(now()) @db.Timestamptz(6)
  parcels_williamson       parcels_williamson @relation(fields: [parcel_id], references: [parcel_id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model stg_attom_property_boundary_travis {
  id          BigInt   @id @default(autoincrement())
  parcel_id   String
  attom_id    String
  county      String?
  source_file String?
  ingested_at DateTime @default(now()) @db.Timestamptz(6)
  raw         Json?

  @@index([attom_id])
  @@index([parcel_id, attom_id], map: "stg_attom_property_boundary_travis_parcel_attom_idx")
  @@index([parcel_id])
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model txgio_centroids {
  prop_id    String?                   @db.VarChar(50)
  owner_name String?                   @db.VarChar(254)
  situs_addr String?                   @db.VarChar(254)
  mkt_value  Decimal?                  @db.Decimal(18, 8)
  land_value Decimal?                  @db.Decimal(18, 8)
  imp_value  Decimal?                  @db.Decimal(18, 8)
  gis_area   Decimal?                  @db.Decimal(18, 8)
  legal_desc String?                   @db.VarChar(254)
  centroid   Unsupported("geography")?

  @@index([centroid], map: "idx_txgio_centroids", type: Gist)
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_parcel_property_travis {
  parcel_id  String
  attom_id   String
  source     String   @default("attom_property_boundary_match")
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@id([parcel_id, attom_id])
  @@index([attom_id])
  @@index([parcel_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_parcel_property_travis_conflicts {
  parcel_id      String   @id
  attom_ids      String[]
  attom_id_count Int
  sample_rows    Json?
  created_at     DateTime @default(now()) @db.Timestamptz(6)

  @@index([attom_id_count(sort: Desc)])
}

model zoning_districts {
  id             Int                      @id @default(autoincrement())
  zoning_code    String?                  @db.VarChar(50)
  zoning_desc    String?                  @db.VarChar(255)
  overlay        String?                  @db.VarChar(50)
  geometry       Unsupported("geometry")?
  raw_attributes Json?
  created_at     DateTime?                @default(now()) @db.Timestamp(6)

  @@index([zoning_code], map: "idx_zoning_districts_code")
  @@index([geometry], map: "idx_zoning_districts_geom", type: Gist)
  @@index([overlay], map: "idx_zoning_districts_overlay")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model opportunities {
  id                     String                 @id @default(dbgenerated("(gen_random_uuid())::text"))
  parcel_id              String                 @unique
  county_fips            String                 @default("48453")
  opportunity_score      Decimal?               @default(0) @db.Decimal(5, 2)
  distress_score         Decimal?               @default(0) @db.Decimal(5, 2)
  offmarket_score        Decimal?               @default(0) @db.Decimal(5, 2)
  value_score            Decimal?               @default(0) @db.Decimal(5, 2)
  distress_breakdown     Json?
  offmarket_breakdown    Json?
  value_breakdown        Json?
  tags                   String[]               @default([])
  reasons_json           Json?
  model_version          String?                @default("1.0")
  scored_at              DateTime?              @default(now()) @db.Timestamptz(6)
  signal_count           Int?                   @default(0)
  acres_calc             Decimal?               @db.Decimal
  market_value           Decimal?               @db.Decimal
  asset_class            String?
  owner_entity_type      String?
  is_absentee            Boolean?               @default(false)
  situs_city             String?
  situs_zip              String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  parcel_features_travis parcel_features_travis @relation(fields: [parcel_id], references: [parcel_id], onDelete: Cascade, onUpdate: NoAction, map: "fk_opportunities_parcel")

  @@index([asset_class], map: "idx_opportunities_asset_class")
  @@index([county_fips], map: "idx_opportunities_county_fips")
  @@index([distress_score(sort: Desc)], map: "idx_opportunities_distress_score")
  @@index([model_version], map: "idx_opportunities_model_version")
  @@index([offmarket_score(sort: Desc)], map: "idx_opportunities_offmarket_score")
  @@index([opportunity_score(sort: Desc)], map: "idx_opportunities_opportunity_score")
  @@index([parcel_id], map: "idx_opportunities_parcel_id")
  @@index([opportunity_score(sort: Desc), county_fips], map: "idx_opportunities_score_county")
  @@index([tags], map: "idx_opportunities_tags", type: Gin)
  @@index([value_score(sort: Desc)], map: "idx_opportunities_value_score")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model signals {
  id                     String                 @id @default(dbgenerated("(gen_random_uuid())::text"))
  parcel_id              String
  county_fips            String                 @default("48453")
  signal_type            String
  signal_subtype         String?
  signal_date            DateTime?              @db.Date
  signal_value           Decimal?               @db.Decimal
  signal_years           Int?
  signal_severity        String?
  source_system          String
  source_id              String?
  source_url             String?
  raw_data               Json?
  is_active              Boolean?               @default(true)
  expires_at             DateTime?              @db.Timestamptz(6)
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  parcel_features_travis parcel_features_travis @relation(fields: [parcel_id], references: [parcel_id], onDelete: Cascade, onUpdate: NoAction, map: "fk_signals_parcel")

  @@index([county_fips], map: "idx_signals_county_fips")
  @@index([is_active], map: "idx_signals_is_active")
  @@index([parcel_id], map: "idx_signals_parcel_id")
  @@index([signal_date], map: "idx_signals_signal_date")
  @@index([signal_severity], map: "idx_signals_signal_severity")
  @@index([signal_type], map: "idx_signals_signal_type")
}

enum UserRole {
  AGENT
  BROKER
  LENDER
  INVESTOR
  DEVELOPER
  WHOLESALER
}

enum ListingType {
  FOR_SALE
  FOR_LEASE
  OFF_MARKET
}

enum ListingStatus {
  ACTIVE
  PENDING
  SOLD
  EXPIRED
  WITHDRAWN
  UNDER_CONTRACT
}

enum PropertyType {
  COMMERCIAL
  RESIDENTIAL
  LAND
}

enum DealStage {
  inbound
  buy_box_match
  underwriting
  loi
  offer_submitted
  under_contract
  closed
  terminated
}

enum Priority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum EntityType {
  PERSON
  LLC
  INC
  LP
  TRUST
  UNKNOWN
}

enum entity_type {
  PERSON
  LLC
  INC
  LP
  TRUST
  UNKNOWN
}

model Session {
  id            String   @id @default(dbgenerated("gen_random_uuid()::text"))
  sessionId     String   @unique @map("session_id")
  userId        String?  @map("user_id")
  state         Json?    @default("{}")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  lastActiveAt  DateTime @default(now()) @map("last_active_at") @db.Timestamptz(6)
  expiresAt     DateTime? @map("expires_at") @db.Timestamptz(6)

  @@index([sessionId], map: "idx_sessions_session_id")
  @@index([userId], map: "idx_sessions_user_id")
  @@index([lastActiveAt], map: "idx_sessions_last_active")
  @@map("sessions")
}

model ClaudeSession {
  id            String   @id @default(dbgenerated("gen_random_uuid()::text"))
  sessionId     String?  @map("session_id")
  userId        String?  @map("user_id")
  model         String   @default("claude-sonnet-4-20250514")
  systemPrompt  String?  @map("system_prompt") @db.Text
  startedAt     DateTime @default(now()) @map("started_at") @db.Timestamptz(6)
  endedAt       DateTime? @map("ended_at") @db.Timestamptz(6)
  messageCount  Int      @default(0) @map("message_count")
  toolUseCount  Int      @default(0) @map("tool_use_count")
  totalTokens   Int      @default(0) @map("total_tokens")
  metadata      Json?
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz(6)
  
  messages      ClaudeMessage[]
  enrichments   ParcelEnrichment[]
  artifacts     Artifact[]

  @@index([sessionId], map: "idx_claude_sessions_session_id")
  @@index([userId], map: "idx_claude_sessions_user_id")
  @@index([startedAt], map: "idx_claude_sessions_started_at")
  @@map("claude_sessions")
}

model ClaudeMessage {
  id              String   @id @default(dbgenerated("gen_random_uuid()::text"))
  claudeSessionId String   @map("claude_session_id")
  messageIndex    Int      @map("message_index")
  role            String   // 'user' | 'assistant' | 'system'
  content         String   @db.Text
  toolUses        Json?    @map("tool_uses")
  toolResults     Json?    @map("tool_results")
  inputTokens     Int?     @map("input_tokens")
  outputTokens    Int?     @map("output_tokens")
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  
  session         ClaudeSession @relation(fields: [claudeSessionId], references: [id], onDelete: Cascade)

  @@unique([claudeSessionId, messageIndex])
  @@index([claudeSessionId], map: "idx_claude_messages_session_id")
  @@index([role], map: "idx_claude_messages_role")
  @@index([createdAt], map: "idx_claude_messages_created_at")
  @@map("claude_messages")
}

model ParcelEnrichment {
  id              String   @id @default(dbgenerated("gen_random_uuid()::text"))
  parcelId        String   @map("parcel_id")
  claudeSessionId String?  @map("claude_session_id")
  enrichmentType  String   @map("enrichment_type")
  enrichmentData  Json     @map("enrichment_data")
  confidenceScore Decimal? @map("confidence_score") @db.Decimal(3, 2)
  sourceTool      String?  @map("source_tool")
  verified        Boolean  @default(false)
  verifiedBy      String?  @map("verified_by")
  verifiedAt      DateTime? @map("verified_at") @db.Timestamptz(6)
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime @updatedAt @map("updated_at") @db.Timestamptz(6)
  
  session         ClaudeSession? @relation(fields: [claudeSessionId], references: [id], onDelete: SetNull)

  @@index([parcelId], map: "idx_parcel_enrichments_parcel_id")
  @@index([claudeSessionId], map: "idx_parcel_enrichments_session_id")
  @@index([enrichmentType], map: "idx_parcel_enrichments_type")
  @@index([verified], map: "idx_parcel_enrichments_verified")
  @@map("parcel_enrichments")
}

model TrainingExportLog {
  id              String   @id @default(dbgenerated("gen_random_uuid()::text"))
  exportType      String   @map("export_type") // 'conversation' | 'enrichments' | 'full'
  sessionIds      String[] @map("session_ids")
  dateRangeStart  DateTime? @map("date_range_start") @db.Timestamptz(6)
  dateRangeEnd    DateTime? @map("date_range_end") @db.Timestamptz(6)
  messageCount    Int?     @map("message_count")
  enrichmentCount Int?     @map("enrichment_count")
  filePath        String?  @map("file_path")
  fileSizeBytes   BigInt?  @map("file_size_bytes")
  checksum        String?  // SHA-256 of exported file
  exportedAt      DateTime @default(now()) @map("exported_at") @db.Timestamptz(6)
  exportedBy      String?  @map("exported_by")
  metadata        Json?

  @@index([exportedAt], map: "idx_training_export_log_exported_at")
  @@index([exportType], map: "idx_training_export_log_type")
  @@map("training_export_log")
}

model Artifact {
  artifactId          String   @id @map("artifact_id") @default(dbgenerated("gen_random_uuid()::text"))
  artifactType        String   @map("artifact_type")
  createdBySession    String?  @map("created_by_session")
  createdByUser       String?  @map("created_by_user")
  queryInput          Json?    @map("query_input")
  queryIntent         Json?    @map("query_intent")
  resultParcelIds     String[] @map("result_parcel_ids")
  dataRetrievalTimestamp DateTime? @map("data_retrieval_timestamp") @db.Timestamptz(6)
  filePath            String?  @map("file_path")
  fileFormat          String?  @map("file_format")
  fileSizeBytes       BigInt?  @map("file_size_bytes")
  fileChecksum        String?  @map("file_checksum")
  templateVersion     String?  @map("template_version")
  status              String?  @default("completed")
  downloadCount       Int      @default(0) @map("download_count")
  regeneratedFrom     String?  @map("regenerated_from")
  claudeSessionId     String?  @map("claude_session_id")
  generatedByTool     String?  @map("generated_by_tool")
  generationPrompt    String?  @map("generation_prompt") @db.Text
  claudeMetadata      Json?    @map("claude_metadata")
  createdAt           DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime? @updatedAt @map("updated_at") @db.Timestamptz(6)
  
  claudeSession       ClaudeSession? @relation(fields: [claudeSessionId], references: [id], onDelete: SetNull)

  @@index([createdBySession], map: "idx_artifacts_session_id")
  @@index([claudeSessionId], map: "idx_artifacts_claude_session_id")
  @@index([artifactType], map: "idx_artifacts_type")
  @@index([status], map: "idx_artifacts_status")
  @@map("artifacts")
}
