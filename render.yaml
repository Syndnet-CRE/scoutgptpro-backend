services:
  - type: web
    name: scoutgpt-backend
    runtime: node
    region: oregon
    plan: starter
    buildCommand: npm install npm install && npx prisma generate && npx prisma db execute --file scripts/drop-user-fk.sql --schema prisma/schema.prismanpm install && npx prisma generate && npx prisma db execute --file scripts/drop-user-fk.sql --schema prisma/schema.prisma npx prisma generate
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: CLAUDE_API_KEY
        sync: false
      - key: FRONTEND_URL
        value: https://your-app.netlify.app
      - key: BACKEND_URL
        value: https://scoutgpt-backend.onrender.com
      - key: PORT
        value: 3001
